description: Internal server error
content:
  application/json:
    schema:
      allOf:
        - $ref: "../../core-api-server.yml#/components/schemas/ErrorInfo"
        - type: object
          properties:
            code:
              type: string
              example: api-0006
            message:
              type: string
              example: failed to get hardware info
