post:
  tags:
    - control
  summary: Reboot device
  description: |-
    Reboots the moducop device. The API returns immediately after the reboot process is started.
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: "../../../core-api-server.yml#/components/schemas/EmptyObject"
    "500":
      $ref: "../../responses/internal_server_error.yml"
